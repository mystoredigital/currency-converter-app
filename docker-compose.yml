version: '3.8'

services:
  currency-converter:
    build: .
    container_name: currency-converter
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
